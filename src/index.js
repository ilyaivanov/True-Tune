import React from 'react';
import {render} from 'react-dom';
// import App from './components/app';
import Base from './components/layout/baseLayout';

render(
    <Base/>, document.getElementById('app')
);