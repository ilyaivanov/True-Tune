import * as React from 'react';
import * as cx from 'classnames';

import Portlet from './Portlet';

export default (props: { artist: Artist }) => (<div className="artist-albums-container grid-container">
    Similar artists page
</div>);

